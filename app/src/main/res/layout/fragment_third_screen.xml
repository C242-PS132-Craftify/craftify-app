<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.onboarding.screen.ThirdScreen">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Discover Creative Potential"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="45sp"
        android:fontFamily="@font/poppinsextrabold"
        android:layout_marginTop="30dp"
        android:layout_marginHorizontal="32dp"
        android:gravity="left"
        android:textColor="@color/lightgreen"
        />

    <TextView
        android:id="@+id/tv_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="32dp"
        app:layout_constraintTop_toBottomOf="@+id/tv_title"
        android:fontFamily="@font/poppinssemibold"
        android:textSize="18sp"
        android:text="Transform your trash into treasure! With Craftify, uncover endless DIY craft ideas tailored to your discarded items. Let’s make creativity sustainable!"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="400dp"
        android:src="@drawable/mockup2"
        app:layout_constraintTop_toBottomOf="@id/tv_desc"
        app:layout_constraintStart_toStartOf="parent"
        />

    <TextView
        android:id="@+id/tv_next"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:fontFamily="@font/poppinsbold"
        android:textSize="18sp"
        android:text="Finish"
        android:layout_marginBottom="30dp"
        android:layout_marginEnd="32dp"
        android:textColor="@color/blue"
        />

</androidx.constraintlayout.widget.ConstraintLayout>